[package]
name = "nd-ndi"
description = "NDI publishing with the built-in GStreamer plugin"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
nd-core.workspace = true
gstreamer.workspace = true
# Local copy with the connection-count and runtime-info additions (see
# vendor/gst-plugin-ndi/PATCHES.md).
gst-plugin-ndi = { path = "../../vendor/gst-plugin-ndi", features = ["static"] }
async-trait.workspace = true
tracing.workspace = true
futures.workspace = true
tokio.workspace = true
